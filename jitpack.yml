before_install:
  - sdk install java 17.0.2-open
  - sdk use java 17.0.2-open
install:
  - ./gradlew clean install -xtest -xsignApiPublication -xsignModPublication -xsignSlimPublication build publishToMavenLocal
